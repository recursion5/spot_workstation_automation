This file exists only because Claude Code specifically looks for a file
named `CLAUDE.md`; the actual instructions for working in this repository
live in `AGENTS.md`, tool-agnostically, so any coding agent can read them
directly.
