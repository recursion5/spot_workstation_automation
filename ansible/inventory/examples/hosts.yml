---
# Example only. Copy to ansible/inventory/local/hosts.yml (gitignored)
# and fill real values. Never put passwords here.
all:
  children:
    pos_workstations:
      hosts:
        specimen-01:
          ansible_host: 192.0.2.10
          ansible_user: Administrator
          ansible_password: "{{ lookup('env', 'SPOT_WINRM_PASSWORD') }}"
          ansible_connection: winrm
          ansible_winrm_transport: ntlm
          ansible_winrm_server_cert_validation: ignore
          ansible_port: 5985
          spot_store: unassigned
          spot_register: specimen-01
          spot_role: back-office
