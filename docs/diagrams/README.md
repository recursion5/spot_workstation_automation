# Diagrams

Phase 1 expected (not yet drawn from specimen evidence):

```text
employee desktop shortcut
        -> SPOTLauncher and/or Citrix Workspace/Receiver  (hypothesis)
        -> hosted SPOT session (Account Key / store / workstation)
        -> Windows printers by logical name
                -> port / port monitor
                -> PnP / USB device
                -> vendor driver
```

SPOTWeb + ConnectLink is a documented alternate path. Confirm which path this specimen uses before drawing a second graph.

Launch-path table and RDS dependency sketch: [DISCOVERY-REPORT.md](../DISCOVERY-REPORT.md).
