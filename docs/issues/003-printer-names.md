# 003 — Exact Windows printer names and USB/LPT mappings

**Class:** Open question (high priority)  
**Status:** open

Vendor defaults: `EPSON`, `Tag` (often Generic/Text Only + `net use LPT1`), `Cash Drawer`. Specimen is not customer-facing; cash drawer object not expected.

Close with `Collect-Printers.ps1` and `Collect-PnpUsb.ps1`. Unproven mappings stay hypotheses.

Refs: Q-020–Q-024.
