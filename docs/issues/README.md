# Local issue fallback

GitHub issue/label APIs returned 403 with the current integration token. Git push via the deploy key works.

These Markdown files are the issue tracker until GitHub Issues is writable. Prefer GitHub issues once that permission exists; do not duplicate casually.
